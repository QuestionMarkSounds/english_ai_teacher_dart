name: english_ai_teacher
description: A sample command-line application with basic argument parsing.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  args: ^2.5.0
  dotenv: ^4.2.0
  langchain: ^0.7.7+2
  langchain_openai: ^0.7.3
  retry: ^3.1.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
